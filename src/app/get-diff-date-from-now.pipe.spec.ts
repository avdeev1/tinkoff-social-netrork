import { GetDiffDateFromNowPipe } from './get-diff-date-from-now.pipe';

describe('GetDiffDateFromNowPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDiffDateFromNowPipe();
    expect(pipe).toBeTruthy();
  });
});
