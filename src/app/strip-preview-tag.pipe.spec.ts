import {StripPreviewTagPipe} from './strip-preview-tag.pipe';

describe('StripPreviewTagPipe', () => {
  it('create an instance', () => {
    const pipe = new StripPreviewTagPipe();
    expect(pipe).toBeTruthy();
  });
});
